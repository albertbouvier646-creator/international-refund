'use client'
import { useState } from 'react'
import { useLocale } from '../../context/LanguageProvider'
export default function ContactSection(){ const { t } = useLocale(); const [name,setName]=useState(''); const [email,setEmail]=useState(''); const submit=(e)=>{ e.preventDefault(); alert(t.contact.thanks); setName(''); setEmail('') }; return (<section id="contact" className="mt-12 max-w-6xl mx-auto px-6 bg-white p-6 rounded shadow"><h3 className="text-xl font-semibold">{t.contact.title}</h3><p className="text-gray-600 mt-2">{t.contact.subtitle}</p><form onSubmit={submit} className="mt-4 grid md:grid-cols-2 gap-4"><input value={name} onChange={(e)=>setName(e.target.value)} placeholder={t.contact.name} className="p-2 border rounded"/><input value={email} onChange={(e)=>setEmail(e.target.value)} placeholder={t.contact.email} className="p-2 border rounded"/><button type="submit" className="bg-blue-600 text-white px-4 py-2 rounded md:col-span-2">{t.contact.send}</button></form></section>) }
